---
type Props = {
  href: string;
  label: string;
};

const { href, label } = Astro.props;
---

<a
  href={href}
  class="bg-secondary grid place-items-center rounded-full px-6 py-2 transition-transform active:scale-95"
>
  <span class="sr-only">{label}</span>
  <slot />
</a>
