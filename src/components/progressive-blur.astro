<div
  class="progressive-blur pointer-events-none fixed right-0 bottom-0 left-0 z-50 w-full"
>
</div>

<style>
  .progressive-blur {
    --p-blur: 8px;
    -webkit-mask-image: linear-gradient(0deg, #000 30%, transparent);
    -webkit-backdrop-filter: blur(var(--p-blur));
    mask-image: linear-gradient(0deg, #000 30%, transparent);
    backdrop-filter: blur(var(--p-blur));
    height: clamp(80px, 10vh, 200px);
  }
</style>
