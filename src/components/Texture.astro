<svg
  class="pointer-events-none fixed inset-0 z-[9999] h-screen w-screen translate-y-0 opacity-20 brightness-40 contrast-60"
>
  <filter id="noise">
    <feTurbulence
      type="fractalNoise"
      baseFrequency=".8"
      numOctaves="4"
      stitchTiles="stitch"></feTurbulence><feColorMatrix
      type="saturate"
      values="0"></feColorMatrix>
  </filter>
  <rect width="100%" height="100%" filter="url(#noise)"></rect>
</svg>
