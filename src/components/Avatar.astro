---
import AvatarImage from "@/assets/images/avatar.png";
import { Image } from "astro:assets";
---

<Image
  src={AvatarImage}
  alt="Hamzat Victor"
  quality={100}
  class="avatar mx-auto size-[7rem] overflow-hidden rounded-4xl border-2 grayscale-70 transition duration-300 ease-out hover:grayscale-0"
/>

<style>
  @property --border-angle {
    inherits: false;
    initial-value: 0deg;
    syntax: "<angle>";
  }
  .avatar {
    background:
      linear-gradient(#121212, #121212) padding-box,
      conic-gradient(
          from var(--border-angle),
          #323435 80%,
          #a3a7ab 86%,
          #c7c9cb 90%,
          #a3a7ab 94%,
          #323435
        )
        border-box;
    border: 2px solid transparent;
    animation: rotate-border 4s linear infinite;
  }
  @keyframes rotate-border {
    0% {
      --border-angle: 0deg;
    }
    100% {
      --border-angle: 360deg;
    }
  }
</style>
